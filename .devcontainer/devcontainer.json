{
    "name": "Codespace",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "runArgs": [
        "--init"
    ],
    // "postStartCommand": "xvfb-run -s '-screen 0 1920x1080x24' code",
    "settings": {
        "terminal.integrated.shell.linux": "/bin/bash"
    },
    "customizations": {
        "vscode": {
            "extensions": [
                "esbenp.prettier-vscode"
            ]
        }
    }
}